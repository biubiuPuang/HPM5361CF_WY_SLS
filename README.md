# MotorMonitor - 双通道工业通信监控与电机控制

## 项目简介
MotorMonitor 基于 **HPM5361EVK** 开发板，用于监听双电机机器人或工业设备的第一路 RS485/CAN 总线信号。当总线空闲超时或急停按钮触发时，自动切断通信链路，并通过第二路 RS485/CAN 控制两台电机返回原点。期间若重新监听到有效信号，立即恢复链路，确保系统快速响应。

## 功能说明
我需要作为一个监听设备,监听主机和双踏板之间的通讯是否正常,
如果正常通讯我什么都不做,如果1秒内没有任何的通讯内容那么我需要立马断开原本的主机和双踏板之间的通讯链接,切换到我这里来由我来控制其双踏板,随后我需要立马控制双踏板回退到原点(这里的回退到原点是原本气缸是往前顶出去的,我需要做的就是把气缸收回来),随后一直等待并监听原本的主机那里有没有通讯传过来,如果主机那边有通讯信息传过来,那么我需要立马断开我和踏板之间的链接且恢复原本主机和双踏板之间的通讯链接,随后我继续监听循环上述的操作即可,监听/切换层我自己做

## 硬件接口定义
| 外设 | 引脚/功能 | 备注 |
|------|-----------|------|
| 第一路 RS485/CAN | UART0 (PA00 TX, PA01 RX, PA09 DE) / CAN3 (PB14 RX, PB15 TX) | 监听外部信号总线信息 |
| 第二路 RS485/CAN | UART2 (PB08 TX, PB09 RX, PA10 DE) / CAN0 (PB01 RX, PB00 TX) | 内部控制电机回退 |
| 继电器控制 | PB10 PB11 | 低电平连接外部通信链路，高电连接内部通信链路；PB10 控制RSD485，PB11 控制CAN|
| 备用输出 | PB12 PB13 | 备用两路继电器，低电平有效|
| 急停按钮 | PA12 PA13 | 读取到低电平连通，两个引脚都可选 |
| 调试串口 | UART6 (PA24 TX, PA25 RX) | 用于日志输出和命令行 |
| 外接系统灯 | PA26 | 状态指示灯 低电平有效|
| 内部系统灯 | PA11 | 状态指示灯 低电平有效|

## 目录结构
MotorMonitor/
├── CMakeLists.txt
├── README.md
├── prj.conf # 项目配置 (Kconfig)
├── src/
|  ├── app/
│  |  ├── main.c # 入口，初始化外设与任务
|  ├── bsp/ # 板级外设驱动
|  |  ├── drv_led.c # 状态指示灯控制
|  |  ├── drv_relay.c # 继电器控制
|  |  ├── drv_uart.c # 串口通信
|  |  ├── drv_can.c # CAN总线通信
|  |  ├── drv_rs485.c # RS485总线通信
|  |  ├── drv_button.c # 急停按钮控制
├── boards/ # 板级外设驱动
│  ├── hpm5361/
│  |  ├── board.c # 板级初始化
│  |  ├── board.h # 板级外设定义
│  |  ├── pinmux.c # 引脚配置
│  |  ├── pinmux.h # 引脚配置
└── build/ # 构建输出目录

## 构建说明
1. **环境准备**  
   - 安装 HPM SDK v1.8.0 及工具链（RISC-V GNU Toolchain）。  
   - 设置环境变量 `HPM_SDK` 指向 SDK 根目录。